To install, this requires Ruby 2.6.6. Install it using [asdf](https://asdf-vm.com/) (`asdf install ruby 2.6.6`). Then, run `gem install bundler`. Then, `bundle install`.

Also, you'll need to run `npm i`
